[project]
name = "civ5-mcp-server"
version = "0.1.0"
description = "MCP server for Civ 5 Bridge"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["mcp>=1.25.0"]

[project.scripts]
civ5-mcp = "civ5_mcp_server:main"
